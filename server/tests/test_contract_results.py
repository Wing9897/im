"""Results contract tests — split for maintainability.

See:
- ``test_contract_results_trending.py`` — trending topics + messages
- ``test_contract_results_events.py`` — events list / sort / filters
- ``test_contract_results_calendar.py`` — calendar occurrence expansion
- ``test_contract_results_queue_stats.py`` — queue + stats
"""
