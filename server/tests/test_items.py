"""Items domain tests — split for maintainability.

See:
- ``test_items_retired_fields.py`` — categories, retired-field reject, worksets
- ``test_items_calendar.py`` — calendar projection, linked expiry cache, agent list_expiring
- ``test_items_inventory.py`` — quantity/unit + agent inventory tools
"""
