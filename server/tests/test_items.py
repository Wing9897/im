"""Items domain tests — split for maintainability.

See:
- ``test_items_attributes.py`` — categories, attributes, normalize, worksets
- ``test_items_calendar.py`` — calendar projection, linked expiry cache, agent list_expiring
- ``test_items_inventory.py`` — quantity/unit/price + agent inventory tools
"""
