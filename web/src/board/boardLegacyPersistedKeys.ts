/**
 * Legacy board layout localStorage key — server ui-prefs is SoT.
 * Test-only: assert leftover LS is ignored on hydrate (not used in production).
 */

export const LEGACY_BOARD_STORAGE_KEY = "im:ops-board:v14";
