import { SubscriptionsIdentityPanel } from "./SubscriptionsIdentityPanel";

/** Calendar-share sign-in, profile, timezone, and sign-out. */
export function SubscriptionsAccountPage() {
  return <SubscriptionsIdentityPanel />;
}
